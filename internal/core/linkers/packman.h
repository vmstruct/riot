#include <core/linkers/stdafx.h>

namespace riot
{
	namespace packman
	{
		void trigger_veh (
			uint64_t address );

		void suspend_process (
			HANDLE process_handle );

		void decrypt_memory_pages_for_dump ( );
	}
}